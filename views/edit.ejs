<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
    <style>
        body {
        font-family: 'Roboto', sans-serif;
        background-color: #f4f4f4;
    }
    
    form {
        width: 70%;
        margin: 0 auto;
        background-color: rgb(230, 172, 113);
        box-shadow: 0 0 10px rgba(206, 104, 104, 0.1);
        padding: 20px;
        border-radius: 5px;
        margin-top: 20px;
    }
    
    form textarea {
        resize: none;
        width: 90%;
        height: 150px;
        padding: 10px;
        margin-bottom: 10px;
    }
    
    form button {
        padding: 10px;
        border-radius: 5px;
        cursor: pointer;
        background-color: rgb(238, 77, 9);
        color: #fff;
        font-weight: bold;
        border: none;
        transition: background-color 0.3s ease;
    }
    
    form button:hover {
        background-color: #b4ff33;
    }
    </style>
</head>
<body>
    <form action="/posts/<%= post._id %>/update" method="post"> 
        <textarea name="review" id="review" rows="10" cols="30" minlength="25"><%= post.review %></textarea >
        <button type="submit">Update</button>
    </form>
</body>
</html>